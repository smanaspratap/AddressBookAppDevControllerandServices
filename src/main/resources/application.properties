# -------------------------------------------------------
# AddressBook Application Properties
# MySQL credentials pre-configured for future DB layer.
# Uncomment when JPA / Repository layer is introduced.
# -------------------------------------------------------
spring.application.name=AddressBookApp
server.port=8080

# spring.datasource.url=jdbc:mysql://localhost:3306/addressbook_db?createDatabaseIfNotExist=true
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.datasource.username=root
# spring.datasource.password=root
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true